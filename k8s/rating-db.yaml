apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rating-db
  template:
    metadata:
      labels:
        app: rating-db
    spec:
      containers:
        - name: mysql
          image: mysql:8
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: rootPassword
            - name: MYSQL_DATABASE
              value: rating_db
          ports:
            - containerPort: 3306

---

apiVersion: v1
kind: Service
metadata:
  name: rating-db
spec:
  selector:
    app: rating-db
  ports:
    - port: 3306
      targetPort: 3306